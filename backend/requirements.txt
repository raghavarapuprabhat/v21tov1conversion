# Backend runtime
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
openpyxl>=3.1
PyYAML>=6.0
SQLAlchemy>=2.0

# Phase 1.5 (PostgreSQL) — safe to keep installed
psycopg[binary]>=3.1

# Tests
pytest>=8.0
httpx>=0.27
