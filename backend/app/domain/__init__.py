"""Domain logic (E2/E4). See LLD §4, §6.

Planned modules:
    linkage.py - per-context IS resolver (Entity/RP_IND/RP_ORG), DD index
    tree.py    - V1 tree from Node+Level, array-node badging, parent-root resolver
"""
